---
title: Deploying to Fluxzero Cloud
description: Explanation on how to deploy your Fluxzero project to Fluxzero Cloud.
sidebar:
   order: 40
---

import { Image } from 'astro:assets';
import {Aside, TabItem, Tabs, Steps} from '@astrojs/starlight/components';

import cloudDashboard from '~/assets/cloud/dashboard-eb.png';

# Fluxzero Cloud

Fluxzero Cloud is a fully managed hosting and observability platform for Fluxzero applications. It handles infrastructure, deployment, monitoring, scaling, and logs—so you can stay focused on writing clean business logic.

> ⚠️ Fluxzero Cloud is currently in private beta. [Request access](/contact) if you’d like to be part of the early adopters.

---

## Why Fluxzero Cloud?

Fluxzero’s primary goal is to make backend development radically simpler and more productive.

The SDK helps you build clean, observable applications. But writing the code is only part of the story—deploying, running, and monitoring your app is just as important.

Fluxzero Cloud gives you:

- GitHub-based deployments (or deploy via API)
- Full audit trail of all incoming and outgoing messages
- Automatic scaling and zero-downtime deployments
- Real-time insights into usage and performance
- Live inspection of application state
- Managed clusters, backups, secrets, and tenant routing

---

## Deploying from GitHub

To create a cluster and deploy your app, all you need is a Fluxzero API key, a GitHub Actions workflow, and a Dockerfile.

<Steps>

1. Create an organization

    Sign in to the Fluxzero Cloud dashboard. Create a new organization if you haven't done already.

2. Download an API key

    Select your organization and open the tab 'API Keys'. Create a new key and download it.

3. Configure API key

    Go to your GitHub repository and create a new Actions secret named `FLUXZERO_API_KEY`. Copy the full content of the key into the 'Secret' field.

4. Add Dockerfile

    Place a file named `Dockerfile`. E.g.:

    ```docker
    FROM eclipse-temurin:25-jre-alpine

    COPY target/app.jar /

    CMD java -jar /app.jar
    ```

5. Run deployment

    Below is a complete example script you can use to deploy an app to a Fluxzero cluster of choice:

    ```yaml
    name: Deploy to Fluxzero Cloud

    on:
      workflow_dispatch:
        inputs:
          cluster-name:
            description: "Cluster name"
            required: true
            default: "Test"
          application-name:
            description: "Application name"
            required: true
            default: "Sample"
          image-name:
            description: "Docker image name"
            required: true
            default: "sample"

    jobs:
      maven-build:
        name: Build
        runs-on: ubuntu-latest

        permissions:
          contents: read

        steps:
          - name: Checkout
            uses: actions/checkout@v5

          - name: Setup Java
            uses: actions/setup-java@v5
            with:
              distribution: 'temurin'
              java-version: '25'
              cache: 'maven'

          - name: Build with Maven
            run: mvn -B -T1C package

          - name: Generate Fluxzero JWT
            id: jwt
            uses: fluxzero-io/fluxzero-jwt-action@v1
            with:
              api-key: ${{ secrets.FLUXZERO_API_KEY }}

          - name: Log in to Fluxzero Container Registry
            uses: docker/login-action@v3
            with:
              registry: registry.fluxzero.io
              username: ${{ steps.jwt.outputs.userId }}
              password: ${{ steps.jwt.outputs.token }}

          - name: Extract metadata
            id: metadata
            uses: docker/metadata-action@v5
            with:
              images: |
                registry.fluxzero.io/${{ github.event.inputs.image-name }}

          - name: Build and push Docker image
            uses: docker/build-push-action@v6
            with:
              context: .
              push: true
              tags: ${{ steps.metadata.outputs.tags }}
              labels: ${{ steps.metadata.outputs.labels }}

          - name: Deploy to Fluxzero cluster
            uses: fluxzero-io/fluxzero-deploy-action@v1
            with:
              cluster-name: ${{ github.event.inputs.cluster-name }}
              application-name: ${{ github.event.inputs.application-name }}
              image-name: ${{ github.event.inputs.image-name }}
              version: ${{ steps.metadata.outputs.version }}
              token: ${{ steps.jwt.outputs.token }}
    ```

    <Aside type="note">
        You can also manually deploy via API using your generated JWT token.
    </Aside>

</Steps>

---

## Dashboard Preview

Below is a snapshot of the Fluxzero Cloud dashboard, showing running applications in a production cluster.

<Image src={cloudDashboard} alt="Fluxzero Cloud Dashboard" style="border-radius: 1.5rem; box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);"/>

The dashboard already supports:

- ✅ Organization & cluster management
- ✅ App deployments and versioning
- ✅ Secrets and environment variables
- ✅ Logs, schedules, and web request inspection
- ✅ Usage monitoring and task breakdowns
- ✅ Multi-tenant routing and domain config
- ✅ User access control and invoicing
- ✅ Data backups and restores

---

## What’s Coming Next

While many features are already live in the private beta, here’s a quick look at what’s on the way:

- Configure endpoints, routing, and tenants directly from the dashboard
- Roll back deployments with a single click
- Auto-scale trackers and applications
- Common admin queries and commands

---

Let us know what you build—and if you have questions or ideas, don’t hesitate to [reach out](/contact). We built this for you!