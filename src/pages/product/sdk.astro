---
import BaseLayout from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import '../../styles/main.scss';
import '../../styles/fluxzero.css';
---

<BaseLayout
        title="SDK | Fluxzero"
        description="Fluxzero is the fastest way to build scalable, message-driven apps. One unified SDK, zero boilerplate, full observability, and deploy anywhere."
>
    <!-- Bootstrap icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.13.1/font/bootstrap-icons.min.css"
          slot="head">

    <!-- Page wrapper for sticky footer -->
    <main class="page-wrapper">
        <div class="bg-dark" style="height: 75px">
            <Header/>
        </div>


        <section class="container fluxzero-product pt-5 pb-2 mt-3 mt-sm-4 mt-xl-5">

            <div class="row gx-5 gy-2">
                <div class="col-md-6">
                    <h1 class="mb-lg-4">Fluxzero SDK</h1>
                    <p>The fastest way to build and scale rock‑solid backends without boilerplate or compromises.</p>

                    <p>
                        The Fluxzero SDK was designed from the ground up to let you build complete backend applications
                        — quickly
                        and joyfully — without touching infrastructure or reinventing plumbing.
                    </p>

                    <p class="fw-bold">
                        You write code that matters to your domain. Fluxzero handles the 50 things you'd rather not
                        implement.
                    </p>

                </div>

                <div class="col-md-6 mb-3 align-self-center text-center">
                    <img src="/assets/fluxzero/fluxzero-product-sdk.svg" alt="Fluxzero SDK" width="502">
                </div>

                <div class="col-md-3"><a href="/docs" class="btn w-100 btn-outline-dark btn-lg">Read the docs</a></div>
                <div class="col-md-3"><a href="/docs/tutorials/first-app" class="btn w-100 btn-primary btn-lg">Build
                    your first app</a></div>

                <hr class="mt-5"/>

                <div class="col-md-12 text-center py-4">
                    <img src="/assets/fluxzero/fluxzero-sdk-features.webp" alt="Fluxzero SDK features">
                </div>

                <hr class="my-1"/>

                <div class="col-md-12">
                    <h3 class="h3 mt-5">Core advantages</h3>
                </div>

                <div class="col-md-6">
                    <ul class="list-unstyled pb-lg-3">
                        <li class="d-flex my-3">
                            <i class="bx bx-check-circle me-2 fs-5 text-primary"></i>
                            <div>
                                <strong>Blazing Developer Velocity</strong>
                                <div class="mt-1">
                                    Build complete backends in hours, not months — fewer moving parts, less code.
                                </div>
                            </div>
                        </li>
                        <li class="d-flex my-3">
                            <i class="bx bx-check-circle me-2 fs-5 text-primary"></i>
                            <div>
                                <strong>Non‑Functionals at Max by Design</strong>
                                <div class="mt-1">
                                    Scalability, observability, resilience, and security aren't just bundled — they're
                                    baked into the
                                    core architecture. You get them at full strength without trade‑offs, while still
                                    moving fast.
                                </div>
                            </div>
                        </li>
                    </ul>

                </div>

                <div class="col-md-6">
                    <ul class="list-unstyled pb-lg-3">
                        <li class="d-flex center my-3">
                            <i class="bx bx-check-circle me-2 fs-5 text-primary"></i>
                            <div>
                                <strong>Single Mental Model</strong>
                                <div class="mt-1">
                                    One unified API for commands, queries, events, workflows, scheduling, persistence —
                                    no juggling
                                    frameworks or paradigms.
                                </div>
                            </div>
                        </li>
                        <li class="d-flex center my-3">
                            <i class="bx bx-check-circle me-2 fs-5 text-primary"></i>
                            <div>
                                <strong>Cloud‑Native by Default</strong>
                                <div class="mt-1">
                                    Develop locally, deploy seamlessly to <a
                                        class="link-primary link-underline-opacity-0"
                                        href="#cloud">Fluxzero Cloud</a> or your own
                                    infrastructure.
                                </div>
                            </div>
                        </li>
                    </ul>
                </div>

                <hr class="my-3"/>

                <div class="col-md">

                    <div class="swiper-slider mx-auto my-5" style="max-width: 900px;">
                        <div class="swiper" data-swiper-options='{
                    "slidesPerView": 1,
                    "effect": "cube",
                    "centeredSlides": false,
                    "loop": true,
                    "speed": 650,
                    "autoHeight": true,
                    "coverflowEffect": {
                      "rotate": 0,
                      "stretch": 0,
                      "depth": 900,
                      "modifier": 1,
                      "slideShadows": true
                    },
                    "autoplay": false,
                    "pagination": {
                      "el": ".swiper-pagination",
                      "type": "fraction"
                    },
                    "navigation": {
                      "prevEl": ".btn-prev",
                      "nextEl": ".btn-next"
                    }
                  }'>
                            <div class="swiper-wrapper">

                                <!-- @formatter:off -->
                                <div class="swiper-slide">
                                    <div data-bs-theme="dark" class="d-block mx-auto rounded-3" style="max-width: 800px;">
<pre class="bg-dark"><code>
<span class="keyword">public record</span> UpdateProject(<span class="annotation">@EntityId</span> ProjectId <span class="field">projectId</span>, ProjectDetails <span class="field">details</span>) {`{`}
    <span class="annotation">@AssertLegal</span>
    <span class="keyword">void</span> <span class="method">assertAuthorized</span>(Project project, Sender sender) {`{`}
    <span class="keyword">if</span> (!sender.authorizedFor(project.owner())) {`{`}
        <span class="keyword">throw</span> ProjectErrors.unauthorized;
        {`}`}
    {`}`}

    <span class="annotation">@Apply</span>
    <span class="keyword">Project</span> <span class="method">apply</span>(Project project) {`{`}
        <span class="keyword">return</span> project.withDetails(<span class="field">details</span>);
    {`}`}
{`}`}

<span class="comment">// Business rules and updates all in one place — declarative and safe.</span></code></pre>
                                    </div>
                                </div>

                                <div class="swiper-slide position-relative" style="top: 150px">
                                    <div data-bs-theme="dark" class="d-block mx-auto rounded-3" style="max-width: 800px;">
<pre class="bg-dark"><code><span class="keyword">public record</span> FindOrders(String <span class="field">term</span>) <span class="keyword">implements</span> Request&lt;List&lt;Order&gt;&gt; {`{`}
    <span class="annotation">@HandleQuery</span>
    List&lt;Order&gt; <span class="method">handle</span>() {`{`}
        <span class="keyword">return</span> Fluxzero.search(Order.<span class="keyword">class</span>).lookAhead(<span class="field">term</span>).fetch(<span class="keyword">100</span>);
    {`}`}
{`}`}

<span class="comment">// This query handles itself, external handlers optional.</span></code></pre>
                                    </div>
                                </div>

                                <div class="swiper-slide position-relative" style="top: 150px">
                                    <div data-bs-theme="dark" class="d-block mx-auto rounded-3" style="max-width: 800px;">
<pre class="bg-dark"><code><span class="annotation">@Test</span>
<span class="keyword">void</span> openCaseTwiceFails() {`{`}
    <span class="field">fixture</span>.givenCommands(<span class="string">"/case/open-case.json"</span>)
           .whenCommand(<span class="string">"/case/open-case.json"</span>)
           .expectExceptionalResult(CaseErrors.<span class="field">alreadyExists</span>);
{`}`}

<span class="comment">// Built-in behavioral testing — no mocks, no setup.</span></code></pre>
                                    </div>
                                </div>

                                <div class="swiper-slide position-relative" style="top: 130px">
                                    <div data-bs-theme="dark" class="d-block mx-auto rounded-3" style="max-width: 800px;">
<pre class="bg-dark"><code><span class="annotation">@HandleSchedule</span>
<span class="annotation">@Periodic(cron = <span class="string">"0 * * * MON-FRI"</span>)</span>
<span class="keyword">void</span> handle(PollNewsFeed schedule) {`{`}
    <span class="keyword">var</span> feed = Fluxzero
        .sendWebRequestAndWait(WebRequest.get(<span class="string">"https://example.org/feed"</span>).build())
        .getPayloadAs(Feed.<span class="keyword">class</span>);
    <span class="comment">// Store new headlines or trigger notification</span>
{`}`}

<span class="comment">// Auto-started schedules. Durable, distributed, and code-first.</span></code></pre>
                                    </div>
                                </div>
<!-- @formatter:on -->

                            </div>

                            <!-- Prev/Next buttons + count -->
                            <div class="d-flex justify-content-center align-items-center pb-3 pt-4 mt-3">
                                <button type="button"
                                        class="btn btn-prev btn-icon position-relative top-0 mt-0 ms-0 me-2"
                                        style="transform: none;" aria-label="Previous">
                                    <i class="bx bx-chevron-left"></i>
                                </button>
                                <div class="swiper-pagination opacity-70 fs-sm fw-medium position-relative top-0 w-auto"
                                     style="min-width: 70px;"></div>
                                <button type="button"
                                        class="btn btn-next btn-icon position-relative top-0 mt-0 ms-2 me-0"
                                        style="transform: none;" aria-label="Next">
                                    <i class="bx bx-chevron-right"></i>
                                </button>
                            </div>
                        </div>
                    </div>

                </div>

                <hr class="mb-5"/>

                <div class="col-md-6">

                    <h3 class="h3">Where Fluxzero shines</h3>

                    <p>Fluxzero is ideal for any business application — from simple internal tools to complex,
                        high-scale
                        systems.</p>

                    <p>Whether you're building a proof-of-concept or a mission-critical platform, Fluxzero lets you move
                        fast
                        without sacrificing quality. You never need to rewrite, re-architect, or rethink how to scale,
                        evolve, or
                        monitor your app.</p>

                </div>

                <div class="col-md-6">

                    <h3 class="h3">Some examples:</h3>

                    <div class="row">
                        <div class="col-sm">
                            <ul class="list-unstyled pb-lg-3">
                                <li class="my-1"><i class="bi bi-car-front me-1"></i> Ride-sharing platforms</li>
                                <li class="my-1"><i class="bi bi-controller me-1"></i> Game servers</li>
                                <li class="my-1"><i class="bi bi-hospital me-1"></i> Healthcare systems</li>
                                <li class="my-1"><i class="bi bi-bank me-1"></i> Government portals</li>
                                <li class="my-1"><i class="bi bi-ticket-perforated me-1"></i> Ticketing systems</li>
                            </ul>
                        </div>
                        <div class="col-sm">
                            <ul class="list-unstyled pb-lg-3">
                                <li class="my-1"><i class="bi bi-building me-1"></i> Booking systems</li>
                                <li class="my-1"><i class="bi bi-box-seam me-1"></i> Supply-chain platforms</li>
                                <li class="my-1"><i class="bi bi-mortarboard me-1"></i> University systems</li>
                                <li class="my-1"><i class="bi bi-bag me-1"></i> E-commerce apps</li>
                                <li class="my-1"><i class="bi bi-bank2 me-1"></i> Banking systems</li>
                            </ul>
                        </div>
                    </div>
                </div>


                <div class="col-md-6">
                    <h3>JVM First. More to Come.</h3>

                    <p>Fluxzero SDK is currently available for Java, Kotlin, and Scala — with full idiomatic support.
                        More
                        languages are on the roadmap, making Fluxzero the go-to backend platform for polyglot teams and
                        future-proof apps.</p>
                </div>

                <div class="col-md-6">
                    <h3>Missing something?</h3>

                    <p>If you're wondering "Can Fluxzero do X?", the answer is probably yes. But if not — we want to
                        know. We're
                        building this for you. <a class="link-primary link-underline-opacity-0" href="#cloud">Let us
                            know what
                            else you'd love to see</a>.</p>
                </div>

                <hr class="mt-4"/>

                <div class="text-center mt-5">
                    <h2 class="h1 mb-4">Ready to Get Started?</h2>
                    <p class="lead pb-3 mb-3">Check out our docs or build your first app</p>

                    <div class="row justify-content-center g-4">
                        <div class="col-md-3"><a href="/docs" class="btn w-100 btn-outline-dark btn-lg">Read the
                            docs</a></div>
                        <div class="col-md-3"><a href="/docs/tutorials/first-app" class="btn w-100 btn-primary btn-lg">Build
                            your first app</a></div>
                    </div>
                </div>

            </div>


        </section>
    </main>

    <Footer/>

    <script>
        import {Swiper} from 'swiper/bundle';
        import 'swiper/swiper-bundle.css';

        // Initialize Swiper
        document.addEventListener('DOMContentLoaded', function () {
            const swiperEl = document.querySelector('.swiper');
            if (swiperEl) {
                const swiperOptions = JSON.parse(swiperEl.getAttribute('data-swiper-options') || '{}');
                new Swiper(swiperEl, swiperOptions);
            }
        });
    </script>

</BaseLayout>